PORT=3001
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_NAME=tech_blog
DB_USER=root
DB_PASSWORD=yourpassword

JWT_ACCESS_SECRET=replace_me
JWT_REFRESH_SECRET=replace_me
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

CLIENT_ORIGIN=http://localhost:3000

# Cookie / CORS hints
# For local dev:
#   COOKIE_SAME_SITE=lax
#   COOKIE_SECURE=false
# For cross-site production (e.g. Vercel frontend + Railway backend):
#   COOKIE_SAME_SITE=none
#   COOKIE_SECURE=true
#COOKIE_SAME_SITE=lax
#COOKIE_SECURE=false
